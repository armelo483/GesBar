import { DateMinorationPipe } from './date-minoration.pipe';

describe('DateMinorationPipe', () => {
  it('create an instance', () => {
    const pipe = new DateMinorationPipe();
    expect(pipe).toBeTruthy();
  });
});
