import { DateMajorationPipe } from './date-majoration.pipe';

describe('DateMajorationPipe', () => {
  it('create an instance', () => {
    const pipe = new DateMajorationPipe();
    expect(pipe).toBeTruthy();
  });
});
